# ── GLPI Database ─────────────────────────────────────────────
# Generate strong passwords: openssl rand -base64 24
GLPI_DB_ROOT_PASSWORD=CHANGE_ME_root_password_here
GLPI_DB_PASSWORD=CHANGE_ME_glpi_password_here

# ── GLPI API (optional — for provisioner) ─────────────────────
# GLPI_URL=http://localhost:80
# GLPI_API_TOKEN=your_api_token_here
# GLPI_APP_TOKEN=your_app_token_here
